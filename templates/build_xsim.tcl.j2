# ── Build Script for Vivado xsim ──
# Auto-generated by vten codegen

# Compile RTL sources
{% for src in rtl_sources %}
xvlog --sv {{ src }}
{% endfor %}

# Compile vten SV library
xvlog --sv {{ vten_sv_dir }}/*.sv

# Compile generated testbench
xvlog --sv {{ generated_dir }}/tb_top.sv

# Elaborate with DPI-C library
xelab tb_top \
    --sv_lib {{ lib_dir }}/libvten_shm \
    -timescale {{ timescale }} \
    -debug typical
